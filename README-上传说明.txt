红祺腾达官网 GitHub Pages 完整代码 V4

本压缩包：
1. 不包含 .git 文件夹；
2. index.html、CNAME、.nojekyll 位于网站根目录；
3. 包含 1612 个项目详情页和 1612 张独立项目图片；
4. 可发布到 GitHub Pages：main 分支 / (root)。

上传时：
- 在 GitHub Desktop 中打开现有 hongqi-tengda-site 仓库；
- 保留仓库自己的 .git 文件夹；
- 删除仓库根目录中除 .git 外的旧网站文件；
- 将本压缩包解压后的全部内容复制到仓库根目录；
- Commit to main，再 Push origin；
- Settings > Pages：Deploy from a branch，main，/(root)。

根目录必须直接看到：
index.html
CNAME
.nojekyll
assets
project
category
board

不要把整个外层文件夹再套进仓库。
